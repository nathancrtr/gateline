# The deploy image needs only packages/ and deploy/ (build context is the
# repository root so both are reachable).
*
!packages
!deploy
packages/node_modules
packages/**/node_modules
packages/web/dist
packages/test-results
packages/playwright-report
