.claude/settings.local.json
node_modules/
__pycache__/
# instance deployment config (copy deploy/fly.example.toml; never commit)
/fly.toml
# local API keys / secrets (e.g. OPENROUTER_API_KEY); never commit
/.env
# local private memos and strategy notes; never commit
/.private/
# Claude Code scratch worktrees; generated per checkout, never committed
/.claude/worktrees/

# Playwright leaves this behind after a run; it is a scratch file, not evidence.
/test-results/

# Finder scatters these through any directory it has been opened in.
.DS_Store

# Public docs site: generated API docs and local tooling are built, never committed
site/api/
site/node_modules/
