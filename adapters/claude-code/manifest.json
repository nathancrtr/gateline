{
  "adapter": "claude-code",
  "output_dir": ".claude/agents",
  "filename": "{role}.md",
  "roles": [
    "analyst",
    "architect",
    "implementer",
    "reviewer",
    "verifier",
    "ops",
    "integrator",
    "historian",
    "ux-researcher",
    "designer"
  ],
  "tools_style": "comma",
  "tool_map": {
    "read": [
      "Read"
    ],
    "search": [
      "Grep",
      "Glob"
    ],
    "write-artifacts": [
      "Write"
    ],
    "edit-code": [
      "Write",
      "Edit"
    ],
    "shell": [
      "Bash"
    ],
    "web-research": [
      "WebSearch",
      "WebFetch"
    ]
  },
  "_comment_models": "Runner-specific spellings of the registry bindings (registry/models.yaml is the authority for WHICH model each role gets; this maps profile -> this runner's model name). Single-vendor harness: P5 vendor pins fall back to lineage split (fable reviews sonnet).",
  "model_map": {
    "frontier-reasoning": "fable",
    "balanced": "sonnet",
    "fast-cheap": "haiku"
  },
  "model_overrides": {},
  "_comment_model_vendors": "Vendor of each runner spelling above, for the orchestrator's dispatch-time P5 check (avoid_vendor_of). Single-vendor harness: every spelling is one vendor, so the pins are satisfiable only via a second adapter (ORCHESTRATOR.md \u00a75.3).",
  "model_vendors": {
    "fable": "anthropic",
    "sonnet": "anthropic",
    "haiku": "anthropic"
  },
  "extra_frontmatter": {},
  "_comment_headless": "v1 orchestrator dispatch (docs/ORCHESTRATOR.md \u00a75). {prompt} is the rendered dispatch prompt; the command runs with cwd at the repo root with the run branch checked out. No model flag: the rendered subagent's frontmatter pins the model, so naming the subagent in the prompt binds the role AND the model. permission_mode is what makes headless runs unattended; operators may narrow it via project settings allowlists (adapter rule: narrow, never widen).",
  "headless": {
    "command": [
      "claude",
      "-p",
      "{prompt}",
      "--output-format",
      "json",
      "--permission-mode",
      "bypassPermissions"
    ],
    "dispatch_prompt": "Use the {role} subagent, running it in the foreground \u2014 wait for it to complete; never launch it as a background agent (a background subagent outlives this print-mode session and its work is lost uncommitted). {body}",
    "usage_report": {
      "format": "json-stdout",
      "fields": {
        "cost_usd": "total_cost_usd",
        "tokens_in": "usage.input_tokens",
        "tokens_out": "usage.output_tokens"
      },
      "error_field": "is_error",
      "result_field": "result"
    }
  }
}
