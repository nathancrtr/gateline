{
  "name": "@gateline/server",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Localhost HTTP layer over @gateline/core: JSON reads, the single decision write path, SSE freshness, and the built SPA.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/nathancrtr/gateline.git",
    "directory": "packages/server"
  },
  "author": "Nathan Carter",
  "exports": {
    ".": "./src/app.ts",
    "./main": "./src/main.ts",
    "./contract": "./src/contract.ts"
  },
  "scripts": {
    "dev": "node src/main.ts"
  },
  "dependencies": {
    "@gateline/core": "*",
    "@hono/node-server": "^1.15.0",
    "hono": "^4.8.0"
  },
  "devDependencies": {
    "@gateline/fixtures": "*"
  }
}
