# Contract: produced by Architect; consumed by exactly one Implementer.
# Amended 2026-07-13 with plan ADR-4 (G1 decline): underscore-preservation case added.

id: 02-unit-tests
title: Unit tests for the pure core (heading detection, fences, slugs, dedup, nesting, empty)
requirements: [R2, R3, R4, R5, R6, R8, R9]

scope: |
  Create apps/mdtoc/test_core.py: pytest unit tests that `import mdtoc` (task
  01 is merged) and exercise the pure functions directly — no subprocess, no
  file I/O (build inputs as inline Python strings; use chr(96) or escaped
  quoting for backtick fixtures). Per plan.md "Test-layout contract": run
  pytest from apps/mdtoc/; do not add __init__.py; do not modify mdtoc.py. Do
  not duplicate task 03's process-level tests (exit codes, stderr, real
  files).

  Test-name contract (plan.md, for AC9.2): individually named tests whose
  names include these substrings — 'heading' (R2), 'fence' (R3), 'slug' (R4),
  'duplicate' (R5), 'empty' (R8). Per plan.md's amended test-name contract,
  the slug tests include the underscore-preservation case below.

  Required coverage (one or more tests per bullet):
  - AC2.1 — extract_headings on '# Title' / '## Section A' / '### Sub A1'
    yields levels 1,2,3 with the right texts, in document order.
  - AC2.2 — '#1234' is not a heading.
  - AC2.3 — '####### Seven' (7 hashes) is not a heading.
  - AC3.1 — backtick-fenced '# not a heading' is excluded; a following
    '## Real Heading' is the only heading found.
  - AC3.2 — identical fixture with ~~~ fences gives the identical result.
  - AC4.1 — slugify('Hello World') == 'hello-world'.
  - AC4.2 — slugify("Don't Repeat Yourself") == 'dont-repeat-yourself'.
  - AC4.3 — slugify('1. What v1 changes — and what it must not') ==
    '1-what-v1-changes--and-what-it-must-not' (em dash removed, double space
    collapses to double hyphen).
  - AC4.4 — slugify of 'Code `example`' (backticks in the text) ==
    'code-example'.
  - Underscore preservation (plan ADR-4 as amended at G1; no spec AC yet —
    the G1 decline notes are the authority) — slugify('foo_bar baz') ==
    'foo_bar-baz' (underscore kept, NOT dropped, matching live GitHub).
  - AC5.1 — render_toc over three 'Overview' headings links them to
    #overview, #overview-1, #overview-2 in document order.
  - AC6.1 — render_toc([(1,'Title'),(2,'Section A'),(3,'Sub A1'),
    (2,'Section B')]) equals the exact four-line string from the spec,
    trailing newline included.
  - AC6.2 — render_toc([(3,'Deep Start')]) is indented exactly 4 spaces
    (absolute level, not renumbered).
  - AC8.1/AC8.2 — extract_headings('') == [] and on headingless body text
    == []; render_toc([]) == '' (zero characters, no newline).

  Constraints: stdlib + pytest only; 3.9-safe per plan ADR-8 (no PEP 604
  annotations).

file_contact_surface:
  - apps/mdtoc/test_core.py

acceptance_tests:
  - "`pytest test_core.py` run from apps/mdtoc/ exits 0 (feeds AC9.1)"
  - "`pytest --collect-only` lists distinct tests whose names include the substrings heading, fence, slug, duplicate, empty (AC9.2)"
  - "Every bullet in scope (AC2.1-2.3, AC3.1-3.2, AC4.1-4.4, underscore preservation per plan ADR-4, AC5.1, AC6.1-6.2, AC8.1-8.2) has a passing test"

depends_on: [01-mdtoc-cli]

status: pending

notes: |
  Round 2 (review-02.md, request-changes, 5 findings) — all 5 addressed with
  new tests in apps/mdtoc/test_core.py; no rebuttals, all findings accepted
  as valid mutation-survival gaps.
  - F1 (no source-hyphen slug fixture): added test_slug_source_hyphen_preserved
    — slugify('Re-entry Vector') == 're-entry-vector'.
  - F2 (no tab fixture pinning predicate filter over \w\s-regex): added
    test_slug_tab_dropped_not_converted_to_hyphen — slugify('a\tb') == 'ab'.
  - F3 (fence cross-dialect close / unclosed-at-EOF untested): added three
    tests — test_fence_mixed_dialect_does_not_close (tilde never closes a
    backtick fence), test_fence_close_requires_at_least_opening_length (a
    closing run shorter than the opening length doesn't close; one >= the
    opening length does), test_fence_unclosed_extends_to_end_of_file.
  - F4 (dedup fixture can't distinguish raw-text keying from base-slug
    keying): added test_duplicate_anchors_keyed_on_base_slug_not_raw_text —
    [(1,'Foo Bar'), (1,'foo bar')] -> #foo-bar / #foo-bar-1.
  - F5 (bare-marker '##' and .strip() semantics untested): added
    test_heading_bare_marker_yields_empty_text (extract_headings('##\n') ==
    [(2, '')]) and test_heading_text_is_stripped_of_trailing_whitespace
    (extract_headings('# Title  \n') == [(1, 'Title')]).
  Result: 24 tests, all passing (`pytest test_core.py` from apps/mdtoc/,
  /usr/bin/python3 3.9.6, pytest 8.4.2 — "24 passed in 0.05s"). AC9.2
  name-substring contract re-verified intact after the additions (heading
  x7, fence x5, slug x7 incl. duplicate-substring overlap, duplicate x2,
  empty x4). Only apps/mdtoc/test_core.py touched; mdtoc.py and test_cli.py
  untouched. Not committed per dispatch instructions.
