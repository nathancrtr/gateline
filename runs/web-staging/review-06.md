# Review Report: 06-e2e-staging-arm

**Verdict:** approve
**Round:** 1 of 3
**Diff reviewed:** commit ebebf50 (branch run/web-staging)

## Findings

### F1 — minor, PLAUSIBLE — the AC7.1 nav-text assertion latently races the inbox badge
- **Where:** `frontend/e2e/staging.spec.ts:92-94` (vs `frontend/packages/web/src/app.tsx:24-29`)
- **Failure scenario:** the fixture repo's inbox is non-empty, so once `/api/inbox` resolves the Inbox NavLink renders a count badge inside the `<a>`, making its text `Inbox<N>`; `toHaveText(['Inbox', 'Portfolio', 'Metrics'])` then retries to the 15s expect timeout and fails. The test passes today only because the assertion beats a cold server's multi-second inbox derivation — a warm cache or faster machine flips it into a spurious red. Fix is one locator refinement (assert the label span, or `toContainText` per link, keeping `toHaveCount(3)` as the AC7.1 discriminator).
- **Requirement:** AC7.1 (the assertion's intent is right; only its text-matching form is fragile)

### F2 — minor, PLAUSIBLE — a hard test-timeout mid-file can leave a stale server answering port 4398's readiness poll
- **Where:** `frontend/e2e/staging.spec.ts:36-56` (spawn + health poll, `afterAll` cleanup)
- **Failure scenario:** a future in-file failure that kills the worker before `afterAll` (the implementer's own notes document hitting exactly this) leaves the old server bound to 4398; the restarted worker's `beforeAll` spawns a second server that cannot listen, yet the health poll succeeds against the stale process serving the old fixture repo, so the remaining chained tests fail with misdirecting git assertions. Inherited verbatim from smoke.spec.ts's idiom, which the task scope prescribes — a diagnosability hazard on failure, not a correctness defect in a passing suite.
- **Requirement:** plan Risk 2 (port/worker interference), scope's "follow smoke.spec.ts's idioms"

## Coverage

Checked clean, with the diff's product — the tests themselves — read for mutant-killing power. **AC7.1**: `nav a` count of exactly 3 fails any mutant adding a top-level route; the entry point is the Portfolio page's "+ New run" link (the mobile nav is a `div`, so the locator correctly sees only the sidebar). **AC1.2 e2e slice / AC4.2**: branch existence, author `Fixture Operator`, verbatim subject including the 36-char client-key suffix (the form unconditionally mints one, `new-run.tsx:67`), and `phase`/`paused_reason`/`staged_by` asserted from `git show` — a mutant mis-writing any of these fails; the full three-profile `parseRunState` round-trip is task 03's surface as the task scope states. **AC2.1/AC2.2 prevention half**: with title, valid slug, and all other inputs valid, only the blank section can hold `ready` false (`new-run.tsx:156`), so the disabled-submit assertion genuinely pins the section pre-check; the refusal wording is server-tested in task 03. **AC6.1/AC6.2**: portfolio row asserts `staged` and not `paused`; the phase-chip locator is correctly narrowed to `header h1 + span` (PhaseChip is the h1's immediate sibling, `run.tsx:86-87`), dodging the false positive from the genesis-preview header's permanent "staged by" provenance line — a subtle trap the implementer diagnosed correctly; arm affordance count 1 and resume count 0 make a resume-offering mutant fail. **AC5.1**: `data-decide="arm"`/`"arm-confirm"` match `decide.tsx:140,241`, the success flash is the panel's `role="status"` element (`decide.tsx:296`), the commit subject is asserted verbatim, and the staged treatment is proven gone after reload. **AC8.1**: the route interception swaps the form's fresh key for the captured genesis key, so the server-side match is what's proven; the assertion discriminates — had the interception silently failed, the same slug on a by-then-armed run returns `slug-taken` (an alert), not the asserted `role=status`. Because test order arms before replaying, the e2e exercises stageRun's any-run client-key scan (`local-source.ts:370-374`); the same-slug still-staged replay branch is covered by task 03's `app.test.ts:275` — between the two suites both classify branches are pinned. **AC8.2**: collision against the non-staged `g0-pending` fixture; the asserted literal `run/g0-pending` comes from the server's `slug-taken` message rendered verbatim, and the tip-oid check catches any silent mutation. **Isolation (plan Risk 2)**: port 4398 is unique to this file, `playwright.config.ts` sets no `fullyParallel` so the file's chained tests run in order in one worker, and the fixture dir/`sourceId()` idiom matches smoke.spec.ts. **Omission of the optional AC5.2 e2e slice**: permitted by the scope's "keep if stable" clause; the refusal behavior is server-tested (`app.test.ts:343`), and the notes' refetch-on-focus diagnosis is consistent with the code. Wording guards: the spec's AC2.3/AC3.2 greps target `frontend/packages/{server,web}/src`, which this diff never touches; run over the diff itself they return nothing. Verified live: `npm install` (node_modules was absent in this checkout), `npm run build`, `npx playwright test` — 13 passed (7 smoke + 6 staging) in 31.4s under the default worker config, reproducing the implementer's claim independently. Not assessed: visual-token form of the staged chip beyond its label (AC6.2's hollow-ring treatment is task 05's reviewed surface; the e2e's label-level distinction matches this task's scope wording).

## Boundary check

Clean. The diff adds exactly the declared `frontend/e2e/staging.spec.ts` plus an append to the task yaml's `notes:` (`status: pending` untouched). No fixture, smoke.spec.ts, or playwright.config.ts changes — the scope's explicit prohibitions all honored.
